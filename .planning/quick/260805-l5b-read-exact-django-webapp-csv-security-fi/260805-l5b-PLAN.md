---
phase: quick-260805-l5b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - djangoexact/package-lock.json
  - djangoexact/requirements.txt
  - .github/workflows/deploy.yaml
  - .github/workflows/deploy-cloudrun.yaml
  - djangoexact/api/tests/test_weasyprint_presentational_hints_guard.py
autonomous: true
requirements:
  - DEP-NPM-01
  - DEP-PY-01
  - CI-PIN-01
  - WEASY-01
user_setup:
  - service: github-dependabot
    why: "The WeasyPrint alert (CVE-2026-49452) must be dismissed as not affected by a human; there is no code change for it"
    dashboard_config:
      - task: 'Dismiss the weasyprint 68.0 Dependabot alert with reason "Vulnerable code is not actually used"'
        location: "GitHub repo -> Security -> Dependabot alerts"

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "npm audit on djangoexact reports zero vulnerabilities"
    - "requirements.txt pins httplib2 0.32.0, pyparsing 3.3.2 and Django 5.2.17"
    - "every uses: reference across .github/workflows is pinned to a commit SHA"
    - "the WeasyPrint not-affected claim is enforced by a DB-free automated test, not just a comment"
    - "package.json direct dependencies are byte-identical to before the change"
  artifacts:
    - djangoexact/package-lock.json
    - djangoexact/requirements.txt
    - .github/workflows/deploy.yaml
    - .github/workflows/deploy-cloudrun.yaml
    - djangoexact/api/tests/test_weasyprint_presentational_hints_guard.py
  key_links:
    - "requirements.txt weasyprint comment states the condition; the guard test enforces it so the claim cannot rot"
    - "httplib2 0.32.0 requires pyparsing<4,>=3.1, so both pins must move together or pip resolution breaks"
    - "package.json stays untouched, so only lockfile-resolved transitive entries move"
---

<objective>
Apply the four actionable remediation surfaces from the Dependabot + Semgrep findings CSV: the npm
transitive dev dependency advisories, the Python dependency pins, the two unpinned GitHub Action
references, and the WeasyPrint not-affected guard.

Purpose: close every verified finding in the CSV without touching the public API, the calculation
layer, or the CI gating configuration.
Output: an updated npm lockfile, three bumped Python pins plus a documented WeasyPrint exemption,
two SHA-pinned workflow steps, and a DB-free regression test protecting the exemption.

Note on task shape: this is a quick remediation plan across four independent surfaces, not a
layered feature, so there is no vertical slice to trace. Tasks are ordered cheapest-to-verify
first and are mutually independent.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260805-l5b-read-exact-django-webapp-csv-security-fi/260805-l5b-CONTEXT.md
@.planning/STATE.md

The CONTEXT.md finding triage is authoritative. Every version number and commit SHA in it was
verified against the live tree, OSV and the npm advisory database on 2026-08-05. Do not re-derive
them and do not query the network to second-guess them.

Environment facts confirmed during planning:
- Working virtualenv at repo root: `.venv` (has Django 5.2.14 and PyYAML 6.0.2 importable).
- `cd djangoexact && ../.venv/bin/python manage.py test api.tests.test_production_config_check`
  runs green with no Postgres, so SimpleTestCase suites are a usable local gate.
- `.github/workflows/` currently has exactly 2 unpinned action references, both
  `docker/setup-buildx-action`, at deploy-cloudrun.yaml:188 and deploy.yaml:350.
- `djangoexact/requirements.txt` currently contains no comment lines; httplib2 is line 8,
  Django line 9, pyparsing line 36, weasyprint line 66.
- 736 Python files live under `djangoexact/` excluding node_modules, venv and __pycache__.

Finding D from the CSV ("Token refresh failed: %s" at accounts/views.py:263) is stale: the
statement exists only on origin/main and was already removed on develop. No task covers it; record
it as resolved-on-develop in the summary.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve the three npm transitive advisories in the lockfile only</name>
  <files>djangoexact/package-lock.json</files>
  <precondition>`npm` is on PATH and outbound network access to the npm registry works from this machine.</precondition>
  <action>
From `djangoexact/`, run `npm audit fix --package-lock-only` to move the three vulnerable transitive
dev dependencies identified in CONTEXT section A: fast-uri (3.1.2, vulnerable 3.0.0 to 3.1.4),
brace-expansion (2.1.0, vulnerable 2.0.0 to 2.1.3) and postcss (8.5.15, vulnerable through 8.5.22).
All three are `fixAvailable: true` with no breaking-change flag.

Do NOT run a plain `npm install`, do NOT run `npm audit fix --force`, and do NOT edit
`package.json`. The `--package-lock-only` flag is what keeps the change scoped to resolved
transitive entries and leaves direct dependencies untouched.

If the command reports any remaining vulnerability that would need `--force`, stop and report it
rather than escalating: a breaking dev-tooling bump is out of scope for this task.

After the command, inspect `git diff --stat djangoexact/` and confirm only `package-lock.json`
changed. Confirm from `git diff djangoexact/package-lock.json` that the moved entries are limited
to fast-uri, brace-expansion, postcss and their own version/resolved/integrity fields, and that no
unrelated dependency tree was rewritten. If the diff is much broader than that, revert with
`git checkout -- djangoexact/package-lock.json` and report instead of committing.

Commit as `fix(deps): resolve npm advisories for fast-uri, brace-expansion and postcss`.
  </action>
  <verify>
    <automated>cd djangoexact && npm audit --audit-level=low; echo "audit_exit=$?"; ../.venv/bin/python -c "import json,sys; json.load(open('package-lock.json')); print('lockfile parses')"; git diff --name-only -- package.json | wc -l</automated>
  </verify>
  <done>`npm audit` exits 0 with zero reported vulnerabilities, `package-lock.json` still parses as JSON, and `git diff --name-only -- package.json` prints 0 lines (package.json untouched).</done>
</task>

<task type="auto">
  <name>Task 2: Bump the vulnerable Python pins and document the WeasyPrint exemption</name>
  <files>djangoexact/requirements.txt</files>
  <action>
Make four edits to `djangoexact/requirements.txt`, using the exact versions locked in CONTEXT
section B. Do not reformat, reorder or otherwise touch any other line in the file.

1. Line 8: change the httplib2 pin from 0.22.0 to 0.32.0 (CVE-2026-59939, decompression bomb).
2. Line 36: change the pyparsing pin from 3.0.9 to 3.3.2. This is forced: httplib2 0.32.0 declares
   `pyparsing<4,>=3.1`, so bumping httplib2 alone would break pip resolution. Neither package is
   imported anywhere in `djangoexact/`, so blast radius is low, and both stay pinned rather than
   being removed so resolution stays deterministic.
3. Line 9: change the Django pin from 5.2.14 to 5.2.17. This stays inside the 5.2 LTS line, so no
   framework-level breaking changes, and it picks up the STARTTLS fix from the CSV plus the six
   further security fixes shipped in 5.2.15, 5.2.16 and 5.2.17.
4. Immediately above the `weasyprint==68.0` pin (currently line 66), add a short comment block
   recording that CVE-2026-49452 does not apply here. The comment must state three things: the CVE
   id, that the advisory only fires when the presentational hints option is enabled and that this
   codebase never enables it (all three call sites use the plain `HTML(string=html).write_pdf()`
   form in `api/views.py`, `api/services/report_jobs.py` and `public/views.py`), and that the
   exemption is enforced by `api/tests/test_weasyprint_presentational_hints_guard.py`. Note that
   the only fix version is 69.0, a major bump that would also move pydyf, tinycss2, cssselect2,
   tinyhtml5 and fonttools under the PDF reporting layer. Keep it to roughly four comment lines.
   The file currently has no comments; standard pip `#` comment lines are fine.

Do NOT run `pip install` for any of these bumps. The sandbox cannot validate them meaningfully and
installing would churn the local environment; editing the pin file is the deliverable and CI or
deploy performs the resolution. Do not change `weasyprint==68.0` itself.

Wording of the comment is yours, subject to the project rule against em-dashes.

Commit as `fix(deps): bump httplib2, pyparsing and Django to patched versions`.
  </action>
  <verify>
    <automated>cd djangoexact && grep -c '^httplib2==0.32.0$' requirements.txt && grep -c '^pyparsing==3.3.2$' requirements.txt && grep -c '^Django==5.2.17$' requirements.txt && grep -c '^weasyprint==68.0$' requirements.txt && grep -c 'CVE-2026-49452' requirements.txt && git diff --numstat -- requirements.txt</automated>
  </verify>
  <done>Each of the five greps prints 1, and `git diff --numstat` shows at most 8 changed lines in requirements.txt (3 pin rewrites plus roughly 4 added comment lines).</done>
</task>

<task type="auto">
  <name>Task 3: SHA-pin the two remaining unpinned GitHub Action references</name>
  <files>.github/workflows/deploy.yaml, .github/workflows/deploy-cloudrun.yaml</files>
  <action>
Two workflow steps still reference a mutable tag; every other `uses:` in the repo is already
SHA-pinned. Replace the floating tag with the commit SHA that tag currently resolves to, taken from
CONTEXT section C: `8d2750c68a42422c14e847fe6c8ac0403b4cbd6f`, which is v3.12.0 released 2025-12-19.

Edit these two lines only:
- `.github/workflows/deploy-cloudrun.yaml` line 188 (inside the "Set up Docker Buildx" step)
- `.github/workflows/deploy.yaml` line 350 (inside the "Set up Docker Buildx" step)

Each becomes a reference to `docker/setup-buildx-action` at that SHA, followed by a trailing
version comment matching the existing in-repo style, which is a `#` plus the version tag, as seen
at `.github/workflows/deploy.yaml:47`. Preserve the existing indentation of each line exactly:
deploy-cloudrun.yaml uses 8 spaces before `uses:` and deploy.yaml uses 6.

Do NOT jump to v4: pinning the major already in use is the minimal change that closes the finding
without altering build behaviour. Leave the explanatory comments above each step untouched, and do
not touch the disabled `test` job guard or the `pip-audit` continue-on-error setting in
deploy.yaml.

Commit as `fix(ci): pin docker/setup-buildx-action to a commit SHA`.
  </action>
  <verify>
    <automated>cd /home/sirvosterzo/Developer/FAO/EXACT/exact-django-webapp && .venv/bin/python -c "import yaml; [yaml.safe_load(open(p)) for p in ('.github/workflows/deploy.yaml','.github/workflows/deploy-cloudrun.yaml')]; print('yaml ok')" && grep -rn 'docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f' .github/workflows/ | wc -l && grep -rEn 'uses:[[:space:]]+[^ ]+@v[0-9]' .github/workflows/ | wc -l</automated>
  </verify>
  <done>Both workflow files still parse as YAML, the SHA-pinned reference count is 2, and the unpinned-reference count is 0 (it was 2 before the change).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add a DB-free guard test protecting the WeasyPrint not-affected claim</name>
  <files>djangoexact/api/tests/test_weasyprint_presentational_hints_guard.py</files>
  <behavior>
    - Scanning the Django source tree with no offending call site present passes.
    - The scan is non-vacuous: it must assert it actually visited a large set of Python files, so a
      broken directory walk cannot make the guard pass silently.
    - The guard file itself is excluded from its own scan, since it necessarily names the option.
    - The test needs no database and no network, so it runs in the constrained local sandbox.
  </behavior>
  <action>
Create `djangoexact/api/tests/test_weasyprint_presentational_hints_guard.py`, following the
conventions of the existing DB-free suite at `djangoexact/api/tests/test_production_config_check.py`
(module docstring, then imports, then a single `SimpleTestCase` subclass).

Module docstring: explain that WeasyPrint is deliberately held at 68.0 despite CVE-2026-49452,
that the advisory is only reachable when the presentational hints option is enabled, and that this
test is what keeps the not-affected claim honest.

Imports: `pathlib.Path`, `django.conf.settings`, and `SimpleTestCase` from `django.test`.
`settings.BASE_DIR` resolves to the Django root `djangoexact/`, which is the correct scan root.

Module-level constants: the option name being searched for, and a set of directory names to skip
while walking. The skip set must cover vendored and generated trees so the guard stays fast and
cannot trip on third-party code: `node_modules`, `venv`, `env`, `.venv`, `.git`, `__pycache__`,
`static`, `media`, `locale`, `logs`.

Class `WeasyPrintPresentationalHintsGuardTests(SimpleTestCase)` with a private helper that walks
`Path(settings.BASE_DIR).rglob("*.py")`, skips any path having a skip-set name among its parts,
skips the path equal to `Path(__file__).resolve()`, reads each file with `encoding="utf-8"` and
`errors="replace"`, and returns a tuple of (list of offending paths relative to BASE_DIR, count of
files scanned).

Two test methods:
1. `test_no_source_enables_presentational_hints` asserts the offender list is empty. The failure
   message must name CVE-2026-49452, list the offending files, and instruct the reader that the
   requirements.txt exemption is now invalid and weasyprint must be bumped to 69.0 or the call site
   otherwise mitigated. Use `assertEqual` against an empty list so the offending paths appear in
   the diff output.
2. `test_guard_scans_a_meaningful_file_set` asserts the scanned-file count is greater than 200. The
   tree currently holds 736 eligible Python files, so 200 is a safe floor that still catches a walk
   that silently collapses to nothing.

Match the flag by plain substring containment rather than a regex on `=True`, so dictionary and
keyword-expansion spellings are caught too. Any occurrence at all is a signal to re-triage, which
is why the guard file excludes itself instead of trying to special-case its own mentions.

Do not add a `__init__.py` or otherwise restructure `api/tests/`; the package already resolves.

Commit as `test(reports): guard the WeasyPrint not-affected exemption`.
  </action>
  <verify>
    <automated>cd /home/sirvosterzo/Developer/FAO/EXACT/exact-django-webapp/djangoexact && ../.venv/bin/python -m py_compile api/tests/test_weasyprint_presentational_hints_guard.py && ../.venv/bin/python manage.py test api.tests.test_weasyprint_presentational_hints_guard 2>&1 | tail -5</automated>
  </verify>
  <done>The file byte-compiles and `manage.py test api.tests.test_weasyprint_presentational_hints_guard` reports `Ran 2 tests` and `OK` without a database.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> build toolchain | Untrusted third-party code enters the frontend build via transitive dev dependencies |
| PyPI -> App Engine / Cloud Run runtime | Untrusted third-party code enters the server runtime via requirements.txt |
| GitHub Actions marketplace -> CI runner | A mutable tag lets an upstream maintainer or a tag-move attacker execute new code with the CI service account and deploy credentials |
| Untrusted HTML -> WeasyPrint | Report HTML is rendered to PDF by a library with a known parser-side advisory |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-l5b-01 | Denial of Service | httplib2 0.22.0 in requirements.txt | medium | mitigate | Task 2 bumps to 0.32.0, which fixes the CVE-2026-59939 decompression bomb; pyparsing moves to 3.3.2 to satisfy the new constraint |
| T-l5b-02 | Tampering / Information Disclosure | Django 5.2.14 | high | mitigate | Task 2 bumps to 5.2.17, closing the STARTTLS CVE from the CSV plus six further LTS security fixes |
| T-l5b-03 | Tampering | docker/setup-buildx-action@v3 in two workflows | high | mitigate | Task 3 pins to commit 8d2750c68a42422c14e847fe6c8ac0403b4cbd6f so a moved tag cannot inject code into the deploy pipeline |
| T-l5b-04 | Denial of Service | fast-uri, brace-expansion, postcss in package-lock.json | low | mitigate | Task 1 resolves all three via a lockfile-only audit fix; all are dev-time transitive deps, not shipped to users |
| T-l5b-05 | Denial of Service | weasyprint 68.0 PDF rendering | medium | accept | Vulnerable path requires the presentational hints option, which is never enabled at any of the three call sites. Accepted with a documented exemption in requirements.txt plus the Task 4 regression guard. Alert to be dismissed as not affected by the user |
| T-l5b-SC | Tampering | npm / pip installs | high | mitigate | No new package names are introduced. Task 1 only moves existing transitive resolutions and asserts package.json is unchanged; Task 2 only edits pin versions of packages already in the tree and performs no install. No legitimacy checkpoint is required because no new dependency enters the project |
</threat_model>

<verification>
Run from the repo root after all four tasks:

1. `cd djangoexact && npm audit --audit-level=low` exits 0.
2. `git diff --name-only origin/develop...HEAD` lists exactly the five files in `files_modified`.
3. `cd djangoexact && ../.venv/bin/python manage.py test api.tests.test_weasyprint_presentational_hints_guard api.tests.test_production_config_check` reports OK (7 tests, no database).
4. `.venv/bin/python -c "import yaml; [yaml.safe_load(open(p)) for p in ('.github/workflows/deploy.yaml','.github/workflows/deploy-cloudrun.yaml')]"` succeeds.
5. `grep -rEn 'uses:[[:space:]]+[^ ]+@v[0-9]' .github/workflows/ | wc -l` prints 0.
6. Project style gate: `git diff origin/develop...HEAD | grep -Pc '\x{2014}'` finds no matches (grep exits 1). This covers code, comments and docs in the diff. Commit messages must be checked the same way with `git log origin/develop..HEAD --format=%B | grep -Pc '\x{2014}'`.

Explicitly NOT run locally: `pip install`, `migrate`, the full Django test suite, or anything
needing Postgres or Docker. Those belong to CI.
</verification>

<success_criteria>
- All three npm advisories resolved with `package.json` byte-identical to before.
- httplib2 0.32.0, pyparsing 3.3.2 and Django 5.2.17 pinned; weasyprint still 68.0 with a comment recording CVE-2026-49452 and the not-affected rationale.
- Zero unpinned `uses:` references remain in `.github/workflows/`; both workflow files still parse as YAML.
- The WeasyPrint guard test exists, is DB-free, passes, and is non-vacuous (scans more than 200 files).
- Four commits, one per remediation surface, conventional-commit formatted, no em-dashes anywhere.
- CI gating configuration (`if: false` on the test job, `continue-on-error` on pip-audit) untouched.
</success_criteria>

<output>
Create `.planning/quick/260805-l5b-read-exact-django-webapp-csv-security-fi/260805-l5b-SUMMARY.md` when done.

The summary must record two items that produce no code change:
1. Finding D from the CSV ("Token refresh failed: %s" logger at accounts/views.py:263) is stale.
   The statement exists only on origin/main and was already removed on develop; it was also a
   Semgrep false positive, since the flagged string is a log message and the interpolated value is
   an exception object. Resolved-on-develop, no action taken.
2. Manual follow-up for the user: dismiss the weasyprint 68.0 Dependabot alert (CVE-2026-49452) in
   GitHub Security as "Vulnerable code is not actually used", citing the requirements.txt comment
   and the new guard test. Do not attempt to dismiss it via tooling.
</output>
